# lib-synology-dsm — integration test environment
# DEPRECATED: .env is no longer used.
# Credentials are loaded from infra-synology-nas.json (KV fields block).
# Set NAS_CREDS_JSON env var to override the default path:
#   NAS_CREDS_JSON=~/.openclaw/workspace/infra/secrets/infra-synology-nas.json
#   pytest tests/integration/ -v
#
# See: tests/integration/conftest.py
