# .devcontainer/.env.local — NAS credentials for integration tests
# Copy to .env.local, fill in values, never commit.
# Delete this file when done with integration testing on this machine.

NAS_HOST=10.6.224.6
NAS_PORT=5001
API_USER=rune-api
API_PASS=
NFS_CLIENT=10.6.224.0/20
FS_BASE_SHARE=by-terraform-state
TEST_USER_PASS=
